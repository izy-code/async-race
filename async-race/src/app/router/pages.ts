export const SUFFIX = '{suffix}';

export enum Page {
  EMPTY = '',
  GARAGE = 'garage',
  GARAGE_WITH_SUFFIX = `garage-${SUFFIX}`,
  WINNERS = 'winners',
  WINNERS_WITH_SUFFIX = `winners-${SUFFIX}`,
}
